export const confirmMessage = {
  titleDelete: (element: string): string => `Eliminar ${element}`,
  descriptionDelete: (element: string): string =>
    `¿Seguro que deseas eliminar ${element} de manera definitiva?`,
};
